name: Check Repo Rules
on:
  push:
    branches:
      - master
      - main
  pull_request:
jobs:
  check-ascii:
    name: Check for non-ASCII characters
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run character check
        run: python3 tools/check-ascii.py
  check-file-length:
    name: Check source file length
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run file length check
        run: python3 tools/check-file-length.py
